{
  "name": "scheme-a-reproduction",
  "version": "1.0.0",
  "description": "Polygon + MongoDB 混合认证架构（原论文复现）",
  "main": "server/index.js",
  "scripts": {
    "start": "node server/index.js",
    "compile": "npx hardhat compile",
    "deploy": "npx hardhat run scripts/deploy.js --network localhost",
    "test": "npx hardhat test"
  },
  "dependencies": {
    "express": "^4.18.2",
    "web3": "^4.2.0",
    "dotenv": "^16.3.1",
    "cors": "^2.8.5",
    "body-parser": "^1.20.2",
    "mongoose": "^8.0.0"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.19.4"
  }
}
