# 性能对比：scheme-7（全去中心化） vs scheme-a（MongoDB 混合）

## 一、实验环境

| 项目 | 配置 |
|------|------|
| 操作系统 | win32 |
| 区块链 | Hardhat 本地节点 (localhost:8545) |
| 运行时 | Node.js v20.17.0 |
| scheme-a | `scheme-a/` — Polygon + MongoDB 混合架构 |
| scheme-7 | `scheme-7/` — 全去中心化（链上唯一真相源 + 边缘内存验签） |
| 测试设备数 | 5 台 |
| 请求次数 | 每设备 3 次在线认证 |

> ⚠️ **黑盒实测数据** — 由 `benchmark.js` 自动采集于 2026-06-18 10:56:36 (UTC+8)
> 以下数据均为实际 API 调用测量，非预估或占位值。5 设备 × 3 次在线认证 / 1 次离线认证 / 2 次 view 验签。

---

## 二、核心指标对比总览

### 2.1 认证延迟对比

| 认证场景 | scheme-a（MongoDB 混合） | scheme-7（全去中心化） | 变化 |
|----------|-------------------------|---------------------|------|
| 设备注册 | **36 ms** | 26 ms | ↓ |
| 在线写链认证 | 58 ms | **59 ms** | 基本持平 |
| 在线 view 验签 | N/A（scheme-a 不支持） | **17 ms** | 仅 scheme-7 |
| 离线认证 | 28 ms（MongoDB 验证） | **15 ms**（ECDSA 验签） | **46%** ↓ |
| 弱网在线认证（RTT 2000ms） | N/A（scheme-a 端点不存在） | 2100 ms | — |
| 弱网离线认证（已预热） | 24 ms | **13 ms** | — |

### 2.2 在线认证响应时间分布

| 百分位 | scheme-a | scheme-7 |
|--------|---------|----------|
| P50 | 54 ms | 52 ms |
| P90 | 80 ms | 80 ms |
| P99 | 87 ms | 98 ms |
| **平均** | **58 ms** | **59 ms** |
| 样本数 | 15 | 15 |

### 2.3 view 验签（scheme-7 专用，不写链）

| 百分位 | scheme-7 |
|--------|----------|
| P50 | 17 ms |
| P90 | 20 ms |
| 平均 | **17 ms** |
| 样本数 | 10 |

### 2.4 离线认证性能数据

| 指标 | scheme-a（MongoDB） | scheme-7（ECDSA 验签） |
|------|-------------------|---------------------|
| 认证验证时间 | 28 ms | **15 ms** |
| 防重放机制 | 一次性凭证 + MongoDB | timestamp 窗口 + 内存 nonce |
| 信任根 | MongoDB 存储凭证 | 设备私钥 + 链上公钥 |
| 断网可用条件 | 凭证未过期 | 公钥已预热且未过期 |

---

## 三、弱网与断网场景

| 场景 | scheme-a | scheme-7 |
|------|--------|--------|
| 弱网在线认证 (RTT 2000ms) | N/A（端点不存在） | 2100 ms |
| 弱网离线认证 (已预热) | 24 ms | **13 ms** |
| 区块链节点断连 | 降级 MongoDB 本地验证 | 已预热公钥可离线认证 |
| 全部断连 | MongoDB 离线凭证可用 | 公钥已预热则可离线认证 |

---

## 四、结论

### scheme-7 核心成效

1. **离线认证更快**：15ms vs 28ms（46% ↓）
2. **完全去中心化**：无 MongoDB，链上为唯一持久化真相源
3. **view 验签可用**：17ms，高频场景无需写链
4. **弱网离线优势**：弱网离线仅 13ms，不受网络 RTT 影响

### 权衡与代价

1. **在线认证延迟相当**：两方案在线写链延迟基本持平（59ms vs 58ms）
2. **缺少 MongoDB 缓存层**：无毫秒级热路径（但 view 验签提供折中方案）
3. **推荐策略**：日常 `verifyAuth` view 验签 + 定期 `authenticate` 写链更新状态

> 📝 数据采集脚本: `benchmark.js` | 数据文件: `benchmark-results.json`